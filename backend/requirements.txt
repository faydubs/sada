fastapi
uvicorn[standard]
sqlalchemy
psycopg2-binary
pydantic-settings
python-dotenv
python-jose[cryptography]
passlib[bcrypt]
bcrypt==4.0.1
python-multipart
faster-whisper
httpx

# ── XGBoost (مطلوب فعلياً — agent/classifier.py يستورده بدون try/except،
#    كان مفقوداً من هذا الملف رغم استخدامه، يكسر استيراد agent/pipeline.py بالكامل بدونه)
xgboost
# scikit-learn مطلوب لأن xgb.XGBClassifier() يعتمد عليه — بدونه يرمي
# ImportError ويُسقط مسار process-audio عند غياب Gemini.
scikit-learn

# ── Gemini (المسار الأساسي للدقّة — تحليل الصوت الأصلي + النص)
google-genai
# tenacity: إعادة محاولة نداءات Gemini (backoff). av: تحويل صوت المتصفح (webm)
# إلى wav لإرساله لـ Gemini. كلاهما كان يأتي عبر اعتماديات أخرى — نثبّته صراحةً.
tenacity
av

# ── VoicePrint (اختياري — للتحقق من هوية الدلال)
speechbrain
torchaudio
numpy

# ── Dev / اختبارات
pytest
